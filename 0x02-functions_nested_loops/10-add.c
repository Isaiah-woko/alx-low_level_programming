#include "main.h"

/**
 *  add - unction that adds two integers and returns the result
 * @a: number to be added
 * @b: number to be added
 * Return: the result
 */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
