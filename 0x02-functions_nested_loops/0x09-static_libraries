#include "main.h"

/**
 * _abs - Entry point
 * Description: a function that computes the absolute value of an integer.
 * @i: the number to be checked
 * Return: 0
 */

int _abs(int i)
{

	if (i < 0)
	{
		return (-i);
	}
	else

	return (i);

}
