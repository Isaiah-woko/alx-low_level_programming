#include <stdio.h>

/**
 * main - prints a string
 * Description: use the function printf to print a string
 * Return: 0
 */

int main(void)
{
	printf("%s", "with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
